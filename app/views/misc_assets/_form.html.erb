<%= form_for(@misc_asset) do |f| %>
  <% if @misc_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@misc_asset.errors.count, "error") %> prohibited this misc_asset from being saved:</h2>

      <ul>
      <% @misc_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

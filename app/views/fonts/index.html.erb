<h1>Listing fonts</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @fonts.each do |font| %>
      <tr>
        <td><%= link_to 'Show', font %></td>
        <td><%= link_to 'Edit', edit_font_path(font) %></td>
        <td><%= link_to 'Destroy', font, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Font', new_font_path %>
